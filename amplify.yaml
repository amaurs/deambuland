version: 1
env:
  variables:
      APPSYNC_API_KEY: $APPSYNC_API_KEY
      APPSYNC_AUTHENTICATION_TYPE: $APPSYNC_AUTHENTICATION_TYPE
      APPSYNC_GRAPH_QL_ENDPOINT: $APPSYNC_GRAPH_QL_ENDPOINT
      REGION: $REGION
frontend:
  phases:
    preBuild:
      commands:
        - npm ci
    build:
      commands:
        - npm run build
  artifacts:
    baseDirectory: build
    files:
      - "**/*"
  cache:
    paths:
      - node_modules/**/*